# Optional: Ember API key for electricity datasets (kept out of git)
# Get a key at https://ember-energy.org/data/api/
EMBER_API_KEY=

# Optional: IRENA installed capacity baselines (solar/wind)
# Source: https://pxweb.irena.org/pxweb/en/IRENASTAT/ (PXWeb)
# Table defaults to the latest known IRENA electricity statistics table.
# IRENA_ELECSTAT_TABLE=Country_ELECSTAT_2025_H2_PX.px
# IRENA_CAPACITY_START_YEAR=2000
# IRENA_CAPACITY_END_YEAR=2024
# Optional: override the “vintage” recorded into `data_points.source_vintage`
# IRENA_CAPACITY_VINTAGE=irena-pxweb:Country_ELECSTAT_2025_H2_PX.px@2025-07-24

# Optional: pin Ember electricity-generation ingestion to a specific year (YYYY)
# If unset, the importer tries to discover the latest available year.
# EMBER_GENERATION_YEAR=2024

# Optional: by default we only ingest Ember generation for years >= 2024 to avoid
# overwriting the OWID energy import. Override if you want Ember for earlier years.
# EMBER_GENERATION_MIN_YEAR=2024

# Optional: override the “vintage” recorded into `data_points.source_vintage`
# EMBER_GENERATION_VINTAGE=ember-electricity-generation@2024

# Optional: pin Ember installed-capacity ingestion to a specific month (YYYY-MM)
# If unset, the importer tries to discover the latest available month.
# Note: Ember installed-capacity is currently solar/wind only (and limited country coverage).
# EMBER_CAPACITY_DATE=2025-12

# Optional: override the “vintage” recorded into `data_points.source_vintage`
# EMBER_CAPACITY_VINTAGE=ember-installed-capacity@2025-12

# Optional: pin OWID energy ingestion to a Git ref (commit/tag/branch)
# OWID_ENERGY_REF=master
# OWID_ENERGY_VINTAGE=owid-energy-data@master
