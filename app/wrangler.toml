name = "convergence-explorer"
compatibility_date = "2025-01-01"
pages_build_output_dir = "./dist"

[[d1_databases]]
binding = "DB"
database_name = "convergence-db"
database_id = "local" # Replace with actual ID after: npx wrangler d1 create convergence-db
